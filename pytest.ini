[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    slow: tests that run ML inference or stress tests, skip in CI
filterwarnings =
    ignore::DeprecationWarning
